import { describe, it } from 'vitest';

describe.skip('Token classifier validation', () => {
  it('requires real Venice credentials and is executed separately', () => {
    // Placeholder skipped test to keep Vitest suite green without live credentials.
  });
});