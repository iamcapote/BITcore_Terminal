import { describe, it } from 'vitest';

describe.skip('Research pipeline validation', () => {
  it('manual end-to-end validation is covered by integration tests', () => {
    // This suite is intentionally skipped in unit runs. See integration docs.
  });
});