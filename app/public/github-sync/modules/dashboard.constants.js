export const ACTION_MAP = Object.freeze({
  verify: 'verify',
  list: 'list',
  fetch: 'file',
  upload: 'upload',
  push: 'push'
});

export const ACTION_LABEL = Object.freeze({
  verify: 'Verify credentials',
  list: 'List directory',
  fetch: 'Fetch file',
  upload: 'Upload active file',
  push: 'Push dirty files'
});
