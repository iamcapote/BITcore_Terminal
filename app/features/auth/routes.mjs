/**
 * Stub auth routes module.
 * Extend with real auth HTTP routes when needed.
 * @param {import('express').Express} app
 */
export function setupRoutes(app) {
  // No‑op for now – placeholder to satisfy import in start.mjs.
}
export default { setupRoutes };
